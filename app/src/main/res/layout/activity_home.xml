<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:background="@drawable/bg_gradient"
    android:padding="24dp"
    tools:context=".HomeActivity">

    <!-- Top Bar: App Logo, Name, and Settings Icon -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="40dp">

        <!-- Logo + Name -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/appLogo"
                android:layout_width="50dp"
                android:layout_height="50dp"
                android:src="@drawable/logo1"
                android:contentDescription="App Logo"
                android:layout_marginEnd="12dp"
                android:background="@drawable/rounded_logo_bg"
                android:padding="4dp" />

            <TextView
                android:id="@+id/appName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Al’Mumin"
                android:textColor="#FFEB3B"
                android:textSize="26sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                android:shadowColor="#80000000"
                android:shadowDx="2"
                android:shadowDy="2"
                android:shadowRadius="5" />
        </LinearLayout>

        <!-- Settings Icon -->
        <ImageView
            android:id="@+id/settingsIcon"
            android:layout_width="35dp"
            android:layout_height="35dp"
            android:contentDescription="Settings"
            android:padding="6dp"
            android:background="@drawable/rounded_bg"
            android:layout_marginStart="16dp" />
    </LinearLayout>

    <!-- Main Feature Buttons -->
    <Button
        android:id="@+id/btnSalahReminder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Salah Reminder / Prayer Timings"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/rounded_button_blue"
        android:layout_marginBottom="20dp"
        android:padding="16dp" />

    <Button
        android:id="@+id/btnBeautifulAyats"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Beautiful Ayats"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/rounded_button_darkblue"
        android:layout_marginBottom="20dp"
        android:padding="16dp" />

    <Button
        android:id="@+id/btnHadith"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Hadith"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/rounded_button_gold"
        android:padding="16dp" />

    <!-- Logout Button -->
    <Button
        android:id="@+id/btnLogout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Logout"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:fontFamily="sans-serif-medium"
        android:background="@drawable/rounded_button_blue"
        android:layout_marginTop="40dp"
        android:padding="16dp" />

</LinearLayout>
